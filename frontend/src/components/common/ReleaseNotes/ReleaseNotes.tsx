/**
 * ReleaseNotes component - previously handled Electron app update checking.
 * Since this is now a pure web app, no update checking is needed.
 * Updates are handled through standard web deployment.
 */
export default function ReleaseNotes() {
  return null;
}
